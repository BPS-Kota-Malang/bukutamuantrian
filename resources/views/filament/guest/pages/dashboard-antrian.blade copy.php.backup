<x-filament::page>
    <div class="grid grid-cols-1 gap-6 md:grid-cols-2 lg:grid-cols-4">
        @foreach($queues as $queue)
            <div class="relative overflow-hidden transition-all duration-300 bg-white border rounded-xl hover:shadow-lg group">
                <div class="absolute inset-0 transition-opacity duration-300 opacity-0 bg-gradient-to-br from-primary-50 to-secondary-50 group-hover:opacity-100"></div>

                <div class="relative p-6 space-y-6">
                    <div class="flex items-center justify-between">
                        <div class="flex items-center space-x-2">
                            <span class="flex items-center justify-center w-8 h-8 text-lg font-semibold rounded-full text-primary-600 bg-primary-50">
                                {{ $loop->iteration }}
                            </span>
                            <span class="text-sm font-medium text-gray-600">Counter</span>
                        </div>

                        <span @class([
                            'px-3 py-1 text-xs font-semibold rounded-full transition-colors duration-200',
                            'bg-yellow-50 text-yellow-700 border border-yellow-200' => $queue->status === 'queue',
                            'bg-blue-50 text-blue-700 border border-blue-200' => $queue->status === 'onprocess',
                            'bg-green-50 text-green-700 border border-green-200' => $queue->status === 'done',
                        ])>
                            {{ Str::ucfirst($queue->status) }}
                        </span>
                    </div>

                    <div class="space-y-4">
                        <div class="space-y-2">
                            <span class="text-sm font-medium text-gray-500">Now Serving</span>
                            <div class="flex items-baseline space-x-2">
                                <div class="text-5xl font-bold tracking-tight text-primary-600 animate-number-slide">
                                    {{ str_pad($queue->number, 3, '0', STR_PAD_LEFT) }}
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="pt-4 mt-4 space-y-1 border-t border-gray-100">
                        <span class="text-sm font-medium text-gray-500">Operator</span>
                        <div class="flex items-center space-x-2">
                            <div class="flex items-center justify-center w-8 h-8 text-sm font-medium rounded-full text-primary-600 bg-primary-50">
                                {{ substr($queue->operator?->name ?? 'UA', 0, 2) }}
                            </div>
                            <span class="text-sm font-medium text-gray-900">
                                {{ $queue->operator?->name ?? 'Unassigned' }}
                            </span>
                        </div>
                    </div>
                </div>
            </div>
        @endforeach
    </div>
</x-filament::page>
